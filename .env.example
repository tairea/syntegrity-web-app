# Copy to .env and fill in. .env is gitignored.
# NOTE: VITE_-prefixed vars are bundled into client-side code and are therefore
# PUBLIC. The two below are public by design (the Supabase publishable/anon key is
# gated by row-level security).
#
# The OpenRouter API key is NOT here: LLM calls go through the Supabase Edge
# Function `openrouter` (supabase/functions/openrouter/index.ts), which holds the
# key server-side so it never reaches the browser. Set it as a Supabase secret:
#   supabase secrets set OPENROUTER_API_KEY=sk-or-... --project-ref <ref>
#
# Same rule for Google Meet API credentials — DO NOT add them here as VITE_*.
# All meet-* edge functions (including meet-create-session-space) read them
# server-side via Deno.env.get / the meet_credentials table. Set with:
#   supabase secrets set \
#     GOOGLE_OAUTH_CLIENT_ID=... \
#     GOOGLE_OAUTH_CLIENT_SECRET=... \
#     GOOGLE_OAUTH_REDIRECT_URI=https://<ref>.functions.supabase.co/meet-oauth-callback \
#     --project-ref <ref>
# No new secrets are needed for the session-level coordination space —
# meet-create-session-space reuses the same three Google OAuth credentials.
# See docs/google-meet-setup.md for the full one-time setup.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
